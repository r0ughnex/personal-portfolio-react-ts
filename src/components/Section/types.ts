export enum SectionType {
  Trans = 'Transparent',
  Light = 'Light',
  Medm = 'Medm',
  Dark = 'Dark',
}

export enum TitleType {
  Default = 'Default',
  Primary = 'Primary',
  Secondary = 'Secondary',
}
